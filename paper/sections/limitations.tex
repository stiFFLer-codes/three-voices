% SOURCE: heuristic_evaluation.md "Inherent limitations" (six entries:
% mother-tier colour reliance; no rendered text alternative for a deaf
% reader; clinician chart/English literacy; images of text; ASHA tier
% English-only; gTTS as an illustrative stand-in) + related_work.md §4
% concessions (role-based tiers vs. Suresh; ASHA comprehensibility
% unproven; no users; mother tier omits magnitude; Maisoon near-identical
% pipeline; the dataset supports no named condition, per Decision Log #21).
% Plus: the 35 conflicting-label feature vectors; the 0.15 threshold as a
% declared design choice with no empirical basis; calibration —
% random-forest class probabilities are uncalibrated votes and the margin
% is a design constant, not a probability statement (Decision Log #24a);
% gTTS non-reproducibility (#10, #18).
% Figure: failure_mode triptych. The adolescent-age paragraph from
% Decision Log #20 appears at the case's FIRST appearance, which is in
% Data & Vehicle, not here.
% STUB.
\section{Limitations}
\label{sec:limitations}

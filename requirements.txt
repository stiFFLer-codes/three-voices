# One Prediction, Three Voices — pinned dependencies
# Install:  pip install -r requirements.txt

# --- Phase 0-2: pipeline + SHAP ------------------------------------------
ucimlrepo==0.0.7
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
joblib==1.6.0          # transitive via scikit-learn; pinned for model.joblib I/O
imbalanced-learn==0.12.3
xgboost==2.1.1
shap==0.46.0
matplotlib==3.9.2

# --- Phase 3: three-tier renderer (voice + visual) -----------------------
gTTS==2.5.3
Pillow==10.4.0

# --- Dev (optional) ------------------------------------------------------
# jupyter==1.1.1
